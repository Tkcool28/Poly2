name: Source Identity Probe

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run source-identity probe
        run: python3 backend/scripts/probe_source_identity.py
